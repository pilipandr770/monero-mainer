# MineWithMe — Добровольный браузерный майнинг Monero

Полнофункциональный веб-сайт для этичного майнинга Monero через браузер с использованием Flask, PostgreSQL и Docker.

## 🚀 Возможности

- **Прозрачность**: Пользователь сам выбирает уровень нагрузки CPU (10-100%)
- **Добровольность**: Майнинг начинается только после явного согласия
- **Статистика**: Реальные данные о хешрейте, шарах и заработке
- **База данных**: PostgreSQL для хранения глобальной статистики сообщества
- **Docker**: Развёртывание одной командой

## 📋 Перед запуском

**ВАЖНО**: Настройте переменные окружения:

1. Создайте файл `.env` на основе `.env.example`:
   ```bash
   cp .env.example .env
   ```

2. Ваш XMR адрес уже прописан в `.env`, но вы можете изменить:
   - `XMR_WALLET` — адрес вашего Monero кошелька
   - `DEV_FEE` — процент комиссии (по умолчанию 15%)
   - `SECRET_KEY` — секретный ключ для Flask (смените в production!)

## 🛠 Установка и запуск

### Вариант 1: Docker (рекомендуется)

```bash
# Перейдите в папку проекта
cd minewithme-flask

# Создайте .env файл (уже настроен с вашим адресом)
cp .env.example .env

# Или используйте существующий .env (адрес уже прописан)

# Запустите контейнеры
docker-compose up --build

# Откройте браузер
http://localhost:5000
```

### Вариант 2: Локальный запуск

```bash
# Установите зависимости
pip install -r requirements.txt

# Настройте PostgreSQL (или измените DATABASE_URL в config.py)

# Запустите приложение
python app.py
```

## 📂 Структура проекта

```
minewithme-flask/
├── app.py                 # Flask-приложение с API
├── config.py              # Настройки (XMR адрес, пул, комиссия)
├── requirements.txt       # Python зависимости
├── Dockerfile             # Образ для веб-приложения
├── docker-compose.yml     # Оркестрация контейнеров
├── init.sql               # SQL-схема (опционально)
├── templates/
│   └── index.html         # Фронтенд (HTML + JS + Tailwind)
└── static/
    └── css/
        └── style.css      # Дополнительные стили
```
.env файл

Все важные настройки хранятся в `.env`:

- `XMR_WALLET` — Ваш адрес Monero кошелька ✅ (уже настроен)
- `DEV_FEE` — Процент комиссии (по умолчанию 15% = 0.15)
- `POOL_URL` — Адрес пула (MoneroOcean для auto-switch альткоинов)
- `SECRET_KEY` — Секретный ключ Flask
- `DATABASE_URL` — Строка подключения к PostgreSQL

### Безопасность

**Для production обязательно смените:**

```bash
# В .env файле
SECRET_KEY=сгенерируйте-случайную-строку-64-символа
DATABASE_URL=postgresql://НОВЫЙ_ПОЛЬЗОВАТЕЛЬ:СЛОЖНЫЙ_ПАРОЛЬ@db:5432/minewithme
```

⚠️ **Никогда не коммитьте `.env` в Git!** (уже добавлен в `.gitignore`)

## 🚀 Быстрый старт

1. **Ваш XMR адрес уже настроен** в `.env`:
   ```
   47p33B681MuTNp6AfgieQsV5TUPGUfKEM1JC2PbCpvEm3mrxUBDpaDe8b3GkQCPXw3cgHHjKxBKLDZaxEptGW5no4rESTx2
   ```

2. **Запустите проект:**
   ```powershell
   cd "c:\Users\ПК\monero-mainer\minewithme-flask"
   docker-compose up --build
   ```

3. **Откройте в браузере:** http://localhost:5000

✅ **Готово! Майнер работает на ваш кошелёк.**

## ⚙️ Конфигурация

### 🌐 API Endpoints

### GET /api/stats
Получить глобальную статистику сообщества.

**Ответ:**
```json
{
  "total_hashrate": 1.234,
  "total_shares": 567890,
  "estimated_xmr": 0.0456
}
```

### POST /api/submit
Отправить статистику майнинга от пользователя.

**Запрос:**
```json
{
  "hashrate": 123.45,
  "shares": 10,
  "estimated": 0.000012
}
```

## 📊 База данных

### Таблица `stats`

| Поле            | Тип     | Описание                           |
|-----------------|---------|-----------------------------------|
| id              | INTEGER | Первичный ключ                    |
| total_hashrate  | FLOAT   | Общий хешрейт сообщества (MH/s)   |
| total_shares    | INTEGER | Всего подтверждённых шаров        |
| estimated_xmr   | FLOAT   | Примерная сумма XMR для разработки|

## 🔒 Этика и легальность

- **Прозрачность**: Пользователь видит все действия
- **Согласие**: Майнинг начинается только после подтверждения
- **Контроль**: Можно остановить в любой момент
- **Честность**: Процент комиссии указан явно

⚠️ **Убедитесь, что это соответствует законам вашей юрисдикции!**

## 🐛 Troubleshooting

### Порт 5432 уже занят
```bash
# Измените в docker-compose.yml
ports:
  - "5433:5432"  # Внешний порт 5433
```

### База не инициализируется
```bash
# Пересоздайте volume
docker-compose down -v
docker-compose up --build
```

### Майнер не запускается
Проверьте консоль браузера (F12). Возможно:
- Не загрузился `crypto-webminer.com/webminer.min.js`
- Неверный адрес XMR кошелька
- Блокировка AdBlock/антивирусом

## 📄 Лицензия

MIT License — используйте как хотите, но на свой страх и риск.

## 🤝 Поддержка

Если проект помог, можете задонатить XMR:  
`47p33B681MuTNp6AfgieQsV5TUPGUfKEM1JC2PbCpvEm3mrxUBDpaDe8b3GkQCPXw3cgHHjKxBKLDZaxEptGW5no4rESTx2`

---

**Версия**: 1.0.0 (Февраль 2026)  
**Майнер**: Crypto-Webminer (MoneroOcean pool)  
**Стек**: Flask 3.0 + PostgreSQL 16 + Python 3.12
